<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/fastnewsnav"
    app:startDestination="@id/home_fr">
    <fragment
        android:id="@+id/favorites_fr"
        android:name="ru.mail.park.fastnews.views.favorites"
        android:label="fragment_favorites"
        tools:layout="@layout/fragment_favorites" />
    <fragment
        android:id="@+id/settings_fr"
        android:name="ru.mail.park.fastnews.views.settings"
        android:label="fragment_settings"
        tools:layout="@layout/fragment_settings" />
    <fragment
        android:id="@+id/home_fr"
        android:name="ru.mail.park.fastnews.views.home"
        android:label="fragment_home"
        tools:layout="@layout/fragment_home" />
</navigation>